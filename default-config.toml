# Configuration file

#--- Mirador
# For single ion, Display:
precursor_mz = 132.0302
frags_mz = [71.015, 72.007, 88.04] # m/z values of fragments (separated by ',' or ';')
frags_intensity = [9030, 1639, 33785] # intensity of fragments (separated by ',' or ';')
rt = 2.1
at = 13.97
ms_run_ids = [1, 3] 

# For a list of ion targets, Export PDF:
ion_targets = 'Example-Ion-Targets.csv' # File name in output folder or with full path to the list of ion targets (e.g., 'C:/Myfolder/My-Ion-Targets.csv')

mz_tolerance_halfwidth = 0.01 # Da, half-width of the m/z tolerance for matching and signal extraction

mzrange = 0.75 # m/z extraction range for figures and CSVs, half window size (+- center)
rtrange = 0.3 # RT extraction range for figures and CSVs
atrange = 1.5 # AT extraction range for figures and CSVs

mz_tolerance_base_peak_profile_spectrum = 0.05 # If number of neighbor peaks within +- this tolerance from base peak is >= than 4, spectra is considered profile mode, otherwise centroid

# Raw MS data export to CSV:
compress_mz = false # Use true to keep a single m/z value (most intense in extracted data) with all intensities summed from multiple m/z with same RT (and AT)
compress_rt = false # Use true to keep a single RT value (most intense in extracted data) with all intensities summed from multiple RTs with same m/z (and AT)
compress_at = false # Use true to keep a single AT value (most intense in extracted data) with all intensities summed from multiple ATs with same m/z and RT

# MZA conversion:
MinIntensityMza = 20
# time-vs-mz images:
TimeVsMzImageMinIntensityPercentage = 10
TimeVsMzImageMaxIntensityCeilingPercentage = 70

#--- PeakQC
# List of precursor ions to be extracted for QC (optional)
ion_targets = 'Example-Ion-Targets.csv' # File name in output folder or with full path (e.g., 'C:/Myfolder/My-Ion-Targets.csv')

mz_tolerance_halfwidth = 0.01 # Da, half-width of the m/z tolerance for matching and signal extraction
precursor_mz_decimals = 5 # Number of decimal places to retain for precursor m/z values
mzrange = 0.75 # Extraction m/z range, half window size of the view (+- center)
rtrange = 0.3 # Extraction RT range
atrange = 1.5 # Extraction AT range

# Error thresholds for detection of outliers and outside tolerance
mz_error_ppm = 15
rt_error = 0.3
at_error = 0.1
abundance_error = 30 # Percentage absolute error, a percentage of the mean of the ion abundance applied as a threshold to report QC ions outside tolerances

AutoTrackedIonsTopN = 4 # Number of auto-tracked ions to detect per sample group
MinIntensityPresencePercentage = 80 # Intensity threshold presence/absence 

mz_tolerance_base_peak_profile_spectrum = 0.05 # If number of neighbor peaks within +- this tolerance from base peak is >= than 4, spectra is considered profile mode, otherwise centroid


# MZA conversion:
MinIntensityMza = 20
# time-vs-mz images:
TimeVsMzImageMinIntensityPercentage = 10
TimeVsMzImageMaxIntensityCeilingPercentage = 70


#--- TandemMatch
# Reference MS/MS library in CSV or MSP:
library = 'Example-Ion-Targets.csv' # File name in output folder or full path (e.g., 'C:/Myfolder/ion_targets.csv')

mz_tolerance_halfwidth = 0.01 # Da, half-width of the m/z matching window for precursor and fragments
precursor_mz_decimals = 5 # Number of decimal places to retain for precursor m/z values
min_fragments = 2 # Minimum number of fragments (MS/MS peaks) required to match a molecule. Use 2+ for a miningful score
min_cosine_score = 0.7 # Minimum cosine score required for a matched molecule, best is 1.0

# Use true to group matches by MZ within mz_tolerance_halfwidth:
consolidate_matches_by_precursor_mz = false # Values from instance with best score are kept, aggregate/concatenate all MOLECULE names in a new column ALLMOLECULES

# TMT - remove '#' in parameters below to process TMT data:
#precursor_mass_tag = 304.207145 # Tag to add as mass shift to precursor m/z in library: TMTpro=304.207145 and TMT10=229.162932
#tmt_reporter_fragments_csv = 'ReporterFragmentsTMTpro.tsv' # File name with full path, columns 'NAME' and 'MZ' must define TMT fragments, e.g. values f01_126C, 126.12776

# Use 0.7 = 70% of computer cores for multithreading:
cpu_fraction_multiprocessing = 0.7 

#--- PeakQuant
# Precursor ions with m/z to be extracted from the MS1 spectra:
ion_targets = 'Example-Ion-Targets.csv' # File name in output folder or with full path (e.g., 'C:/Myfolder/My-Ion-Targets.csv')

mz_tolerance_halfwidth = 0.01 # Da, half-width of the m/z window for signal extraction
precursor_mz_decimals = 5 # Number of decimal places to retain for precursor m/z values


#--- Comparador
# Folder where CSV files with lists of features are:
folder_lists_features = 'E:\IonToolPack\code\test_data\Lists-Features' # Full path (e.g., 'C:/MyListsOfFeatures/')

delimiter_method_file_name = '__' # Delimiter to get the method name from the CSV file names (e.g., '20CE' from file name '20CE__features.csv')
mz_tolerance = 0.005 # Da, half-width of the m/z tolerance
rt_tolerance = 0.3
at_tolerance = 0.1

# Aliases of column names to harmonize outputs from different software tools:
mz_column_names = ['m/z', 'mz', 'm.z', 'MZ', 'Precursor m/z']
rt_column_names = ['RT', 'RetentionTime', 'RT (min)']
at_column_names = ['AT', 'DT', 'Mobility', 'CCS']
abundance_column_names = ['Intensity', 'Abundance', 'Area', 'Abund']
molecule_column_names = ['Sequence', 'Peptide', 'Molecule', 'MOLECULE', 'Title']

# Use 0.7 = 70% of computer cores for multithreading:
cpu_fraction_multiprocessing = 0.7 
